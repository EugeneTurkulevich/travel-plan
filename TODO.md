# TODO — тулкіт

_Оновлено 2026-08-08 · 1 відкритий пункт._

Борги самого інструмента. Те, що стосується конкретних місць і маршрутів, —
у `TODO.md` кореня даних: сюди воно не потрапляє за визначенням, і
`scripts/check_no_data.py` за цим стежить.

## Гігієна

- [ ] **Видалити заміщені скрипти.** `check_marks.py` (заміщений
      `check_threads.py`, який читає ще й треди місць) і `merge_cloud_photos.py`
      (поглинутий `pull_map.py`). Прибрати після кількох циклів публікації
      новим каналом — не раніше, щоб було куди відкотитись.

      Перевірено 08.08.2026: умова ще не виконана. `plan_push.py` зараз
      показує порожній план, але це знімок поточного стану, не доказ
      публікацій — за `CLAUDE.md` §3 реальні зміни й досі йдуть через
      старий канал (`../travel202609`), `cloud_push.py` (виконавець нового
      каналу) сам ще «на переробку (Ф5)» і жодного разу не виконував пуш.
      Циклів публікації новим каналом було нуль. Повернутись після Ф5.

