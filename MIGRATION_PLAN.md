# План робіт — travel-plan

Виконує рішення зі [`STRUCTURE_PROPOSAL.md`](STRUCTURE_PROPOSAL.md) (v3,
узгоджено 04.08.2026). Порядок фаз обраний так, щоб:

- **нічого незворотного** не сталось до гейту 2 (§Ф5);
- `travel202609` лишався **повноцінно робочим** увесь час, а не «до фази N»;
- кожна фаза мала **перевірюваний критерій приймання**, а не «здається, готово».

Легенда: 🤖 роблю сам · 👤 потрібне твоє рішення або перевірка · ⏱ орієнтовний обсяг.

---

## Ф0 · Каркас двох дерев ⏱ мала

Нічого не переносимо — тільки створюємо порожні місця.

1. `git init` у `travel-plan`; `.gitignore` (`.env*`, `.travel-local.json`,
   `__pycache__`, `.DS_Store`).
2. `git init` у `../travel-data` — **окремий приватний репозиторій** (remote за
   бажанням, можна лишити локальним).
3. `scripts/trip_ctx.py` — резолвер: `--data` → `$TRAVEL_DATA` →
   `.travel-local.json` → `../travel-data/`. Єдина точка, що знає шляхи.
4. `scripts/trip.py show|use` + `.active-trip`.
5. Каркас даних: `places/`, `trips/2026-09-balkans/`, `cache/`. Скопіювати
   `.env` з `travel202609`.
6. `trip.json` — **тільки вересневий** профіль, `map_id` `zb87h25mvtdd8959f2zc`.
   Травневого профілю не створюємо (див. Ф6): травень входить у бібліотеку як
   місця, без маршруту.
7. Скрипти з `travel202609` копіюються як стартова база — **ще без рефакторингу**.

**Приймання:** `python3 scripts/trip.py` друкує активний профіль, шлях до даних і
`map_id`. Обидва дерева під git. У `travel202609` не змінено нічого.

---

## Ф1 · Читання карти ⏱ мала-середня · залежить від Ф0

Робимо **раніше за все інше**: read-only, нічого не пише, і одразу дає незалежний
знімок реальності, на який спираються всі наступні фази.

1. `scripts/pull_map.py` — `get_map` · `get_route` · `list_alt_route` ·
   `list_marks` · `get_places` → `trips/<id>/live_mirror.json`.
2. У знімок обовʼязково: `rev`, `booked`, `ratings`, `threads`, `links`,
   `timing_stale` на точках, `server_tools_rev`.
3. Людиночитаний звіт: скільки точок, POI, фото, альтернатив, звʼязків,
   відкритих міток, чи є заброньовані точки.

4. Заодно `list_my_maps` — довідково: які карти взагалі є і в якому стані
   (травнева `hl5omw94x6hz4p33ekts` лишається на сервері, локального профілю їй
   не заводимо — Ф6).

**Приймання:** знімок збігається з очікуваним станом. ✅ **Виконано наперед
04.08.2026** прототипом (`mcp_probe` + `compare_route.py`), результат:

| | Карта | Локально |
|:--|:--|:--|
| точки | 32, `rev=13` | 32 |
| `date`/`kind`/`arrive_at`/`stay_minutes`/POI/`popup_html` | — | **збігаються повністю** |
| `id` точок | 32/32 | **0/32** |
| альтернативи | 20 (POI 3–16 на точку) | 20 карток у смузі `8100–8750` |
| мітки | 5, усіх `open` = 0 | — |
| треди | 20 — **усі на альтернативах**, на маршруті 0 | немає |
| звʼязки | 1 (Альба-Юлія → Сігішоара, alt↔alt) | немає |
| рейтинги | Белоградчик 4★, Керкіні 3★ (+1 на альтернативі) | немає |
| `booked` | 0 | — |

`rev 13` — наш власний `set_route` 03.08 о 19:24, той самий пробіг 5441.9 км, що
й `rev 12`. **Сторонніх змін немає**, розбирати нічого.

---

## Ф2 · Скрипти беруть шляхи й карту з контексту ⏱ середня · залежить від Ф0

✅ **Виконано 04.08.2026.** Скрипти перестали тягнути за собою мою поїздку.

| Що | Куди | |
|:--|:--|:--|
| `STATE` · `PLACES` · `ENV_FILE` · `DRAFT_ID_FILE` · `CACHE` (7 скриптів) | `trip_ctx` | ✅ |
| `MAP_ID` — **два** літерали: `check_marks.py:32` і `merge_cloud_photos.py:51` | `trip.json` | ✅ |
| `ROOT` (`audit_coords.py:21`, мертвий абсолютний шлях) | `trip_ctx` | ✅ |
| `worker` | `trip.json`, з пріоритетом оточення й `.env` | ✅ |
| `User-Agent: travel202609-…` ×5 | `trip-map-toolkit/1.0` | ✅ |

Другий `MAP_ID` знайшовся лише скануванням — у первинному аналізі я бачив
тільки один. Тому гейт `check_no_data.py` (Ф7) потрібен не «для інших», а нам:
очима такі речі пропускаються.

**Приймання:** `grep` по `scripts/` не знаходить `zb87h…`, `travel202609`,
`Documents/GitHub`; усі модулі компілюються; `check_marks.py` наживо пройшов
ланцюжок `trip_ctx → cloud_push(.env, worker) → воркер` і повернув 5 міток.

### Перенесено далі — залежить від `route.json`, якого ще немає

Ці переїзди неможливі до Ф3, бо цільового файлу не існує. Записано чесно, а не
«зробимо якось потім»:

| Що лишилось у коді | Куди · коли |
|:--|:--|
| `DAYS` (`build_schedule.py:36`) | `route.json` · **Ф4** |
| `ROUTE_PLAN` + словник координат (`osrm_legs.py:188`) | виводиться з `route.json` · **Ф4** |
| `BORDER_FILES` (`generate_state_from_md.py:35`) | `route.json` → `kind: "border"` · **Ф4** |
| `CET_LABELS` (там само, зараз порожній) | `trip.json` → `tz_overrides` · **Ф4** |
| `QUERY_OVERRIDES` (`find_poi_photos.py:91`) | дані бібліотеки · **Ф4** |
| глоб `[0-9][0-9][0-9][0-9].md` у 4 скриптах | реєстр за рядком `ID` · **Ф3**, коли картки його матимуть |

---

## Ф3 · Бібліотека: вересень ⏱ **велика, головна ручна робота** · залежить від Ф1, Ф2

1. `schemas/PLACE_SPEC.md` — секції, три глибини, механізм категорій
   (секція ↔ `type`/`type_name`/`type_icon`), правило `needs_overnight`
   («ніколи не вгадуй»).
2. `schemas/trip.schema.json`, `schemas/route.schema.json`.
3. `scripts/migrate_cards.py` — механічна частина: `0100.md` → `places/<slug>.md`,
   додати `ID` і `Глибина`, зняти `Тип`/`Тайминг`/`Дата`/`Бронь`/`Час перебування`/
   `## Суміжні місця`, винести їх у `route.json`.
4. **Ручний розкол прози** — 30 карток: `## Опис` і `## Нотатки` розділяються на
   позачасове (бібліотека) і день-індексоване (`overlay/<slug>.md`).
   👤 Партіями по 5–8, показую результат кожної партії.
5. `route.json` збирається з `INDEX.md`.

> ### ⚠️ Розводимо прозу **тільки для активного маршруту**
>
> Ручний розкол коштує дорого, і платити за нього варто лише там, де це дає
> результат — тобто для 30 карток **вересневого маршруту**. Усе інше (травнева
> поїздка, смуга `8100–8750`) — це вже **не маршрут, а джерело зібраної
> інформації про місця**: координати, POI, фото, паркування, практичне. Дані
> там неактуальні як план, але цінні як знання.
>
> Для них — механічний імпорт **без маршруту взагалі** (Ф6):
> - trip-рядки (`Тип`/`Тайминг`/`Дата`/`Бронь`/`Суміжні місця`) знімаються
>   скриптом;
> - день-індексована проза не переписується, а **карантиниться** в
>   `## Нотатки (архів MM.YYYY)` — генератори цю секцію ігнорують, тому в попап
>   вона не потрапить ніколи;
> - картка отримує `Глибина: базовий` і позначку «проза не розколота».
>
> Коли таке місце колись заходить в активний маршрут — `/place` розводить прозу
> **тоді**, для однієї картки. Це рівно принцип методички: нарощувати глибину
> лише для того, що дійшло до короткого списку, а не «про всяк випадок».

**Приймання:** жодна картка **вересневого** маршруту не містить дати,
`D<номер>`, «цього разу», «дві ночі»; `route.json` валідний за схемою; кількість
точок збігається зі знімком Ф1.

---

## Ф4 · Генератори ⏱ середня-велика · залежить від Ф3

1. `generate_state.py` — бібліотека + overlay + `route.json` → `route_state.json`,
   з категоріями з `poi_categories`.
   ⚠️ **Не виводити міжкарткові markdown-посилання в `popup_html`.** Виявлено
   04.08.2026: у попапі Літохоро на живій карті висить `](1150.md)` — глядач
   карти бачить посилання на файл репозиторію, перейти нікуди не може.
   Посилання між картками — навігація для нас, не контент для карти: у попапі
   лишається лише текст. Виправити разом із першим пушем (Ф5).
2. `generate_docs.py` — `ROUTE.md`, `CATALOG.md` (реєстр + історія використань +
   глибини), таблиці README.
3. `check_limits.py` — 60 POI/точку · 120 точок · `popup_html` ≤20000 · 900 КБ
   **у байтах**.
4. `migration_diff.py` — структурне порівняння двох `route_state.json` (stdlib).
   ⚠️ **Нормалізувати `nights`:** сервер віддає `nights: 1` для кожної точки, а
   локально поле відсутнє скрізь, крім багатонічних. Без нормалізації прототип
   дав **30 фальшивих дельт із 32** — саме та пастка, яка змусила б шукати
   поломку там, де її немає.

**Приймання — гейт 1 (дешевий):** diff проти
`travel202609/places/exports/route_state.json` порожній, окрім замороженого
списку свідомих дельт. Це **не** критерій перемикання — це спосіб не тягти явні
поломки далі.

---

> ### 📍 Тут зупинились — 04.08.2026
>
> **Зроблено:** Ф0, Ф1, Ф2, Ф3 повністю; Ф4 — по суті, всі чотири скрипти
> написані, прогнані й прийняті.
>
> **Гейт 1 пройдено за змістом:** 11 структурних розбіжностей і 12 «втрачених»
> речень вичитані поштучно — усі 23 є наслідком ручного переписування Ф3,
> справжніх втрат нуль. Обґрунтування кожної — у
> `../travel-data/_migration/accepted_deltas.json`. Єдину справжню втрату,
> яку знайшов гейт (14 блоків бронювання), відновлено й звірено з еталоном
> дослівно.
>
> **Не дороблено одне:** `migration_diff.py --accepted <файл>` — щоб повторний
> прогін друкував «очікуваних 23, несподіваних 0» замість переліку вже
> вичитаного. Вимога до реалізації записана: список не має стати глушником —
> при перевищенні замороженої **кількості** втрат для точки гейт зобовʼязаний
> падати, навіть якщо точка є у винятках.
>
> **Наступний крок:** або дописати `--accepted`, або одразу Ф5 — гейт 1
> одноразовий, а справжній критерій перемикання (порожній план публікації)
> попереду.

## Ф5 · Канал публікації і ПЕРЕМИКАННЯ ⏱ велика · залежить від Ф4

1. `plan_push.py` — diff локального наміру проти `live_mirror.json` → **план**:
   що йде в `demote_points`, що в `promote_alt_points`, що в `swap_route_alt`,
   що в `update_point`/`add_poi`, а що лишається на `set_route`.
2. Гейти всередині плану: усі `booked` id присутні · ліміти · `timing_stale`
   переглянуто.
   ⚠️ `check_limits.py` має бути зеленим **до** першого пушу: три `photo_url`
   перевищують ліміт воркера (див. Ф8). Полагодити разом із першим коммітом
   після гейту, інакше пуш ризикує впасти на валідації.
3. `cloud_push.py` переробляється на **виконавця плану** (був — «залий усе»).
4. `check_threads.py` — мітки **і треди місць** (заміна неповного `check_marks.py`).

**Момент перемикання** — послідовність, не дата:

1. 👤 Останню реальну зміну робимо **старим репо** (якщо на той момент буде що
   міняти). Карта — в стані, який вважаємо правильним.
2. 🤖 `pull_map.py` — новий тулкіт читає карту сам і показує, що бачить.
3. 🤖 `plan_push.py --dry-run` — **план має бути порожній**.
4. 👤 Не порожній → розбираємо кожну позицію: наша поломка чи справді
   розбіжність наміру. Порожній → кажемо «все ок».
5. 🤖 Перший реальний пуш новим тулкітом — свідомо **дрібний** (фікс `_inline_md`
   у блоці 🛍️, він і так у боргах), щоб перевірити канал на малому.

**Приймання — гейт 2 (остаточний):** порожній план, потім успішний дрібний пуш і
`pull_map.py`, що показує очікувану нову ревізію.

**Відкат:** `travel202609` лишається валідним, карта версіонується
(`list_route_revisions` / `get_route_revision`). Незворотних кроків немає.

---

## Ф6 · Травень як джерело знань про місця ⏱ **мала-середня** (було: середня) · після перемикання

Після уточнення про Ф3 ця фаза стала помітно дешевшою: **маршруту не
реконструюємо взагалі**, тільки місця.

1. 33 картки з `travel2026/archive/may/places/` + 14 зі смуги `8100–8750`
   вересневого репо → бібліотека, **механічно**: зняти trip-рядки, карантинити
   день-індексовану прозу в `## Нотатки (архів 05.2026)`, поставити
   `Глибина: базовий` і позначку «проза не розколота».
2. **Дедуп**: Брашов, Сібіу, Русе, Відін та інші повторювані міста зливаються в
   **одну** картку; глибина = найбільша з наявних, контент — обʼєднання.
   👤 Спірні злиття показую.
3. **`route.json` травня НЕ створюємо. Профілю `trips/2026-05-balkans/` теж не
   буде.** Травневий план неактуальний; цінність архіву — місця, не маршрут.
4. Замість генерованої історії використань — **статичний рядок у картці**,
   вписаний один раз при імпорті з травневого `INDEX.md`:

   ```
   ## Історія
   - 05.2026 — були (ночівля D8)      ← статична нотатка імпорту, не генерується
   ```

   Цього досить, щоб правило «міста з травня не беремо як ночівлі» лишалось
   перевірюваним, і не треба тримати фальшивий профіль поїздки заради історії.
5. Травнева карта `hl5omw94x6hz4p33ekts` лишається на сервері як є. Якщо колись
   знадобиться подивитись на неї локально — `trip.py import <map_id>` збудує
   профіль **на вимогу** (§5.5 пропозиції). Тримати його постійно нема сенсу.
6. `travel2026` → read-only архів.

**Приймання:** жодного дубля міста в `places/`; у `CATALOG.md` видно, які місця
прийшли з травня; генератори не виводять карантинні секції в попапи.

---

## Ф7 · Публічність ⏱ середня · після Ф5

Те, заради чого репо взагалі публічне.

1. `.claude/skills/`: `trip` · `place` · `plan-day` · `publish` · `audit` ·
   `alt` · `handoff`.
2. `trip.py import <map_id>` — онбординг: реконструкція профілю й бібліотеки з
   карти (§5.5 пропозиції). Картки — рівень `базовий` з позначкою «проза не
   розколота».
3. `CLAUDE.md`-ядро (методичку воркера не дублює), `docs/PIPELINE.md`,
   `docs/HANDOFF.md`, `schemas/COUNTRY_SPEC.md`.
4. `check_no_data.py` — повний гейт публічності, включно з перевіркою, що в
   `schemas/` немає наших категорій (🍦/🍺).
5. `examples/demo-trip/` — 2–3 вигаданих точки.
6. README для чужої людини: клонуй → підключи MCP → `/trip new` або
   `/trip import`.

**Приймання:** `check_no_data.py` зелений; на демо-профілі проходять `/trip`,
`/place`, `/publish --dry-run` без жодного файлу з моїх даних.

---

## Ф8 · Борги ⏱ розтягнута · паралельно, після перемикання

Не блокують нічого, робляться порціями.

- Фікс `_inline_md` у `_local_block` (він же — перший пуш у Ф5).
- **Три `photo_url` довші за ліміт воркера 500 символів** — знайдено
  `check_limits.py` 04.08.2026: Вірменська церква (Чернівці) 723, Белоградчицькі
  скелі 591. Причина: кириличні імена файлів на Wikimedia кодуються по 6
  символів на літеру, а в thumb-URL імʼя повторюється двічі — у шляху і в
  `/500px-…`. Ці посилання **вже на карті** (воркер їх прийняв на rev 13), але
  схема `set_route` оголошує `maxLength: 500`, тож суворіша валідація відхилить
  наступний пуш. Лікується формою `Special:FilePath/<імʼя>?width=500` — імʼя
  входить один раз, довжина падає удвічі.
- 25 POI без фото · аудит координат 75 POI (43 без латинської назви, 32 без
  відповідника в OSM).
- Крафт-заглушки «крафту немає» — перевірити, чи справді немає.
- 👤 Паром Астакос ⇄ Пісаетос і статус Трансфагарашу — сезонні перевірки з TODO.
- `/handoff` кандидатів у воркер: `audit_coords_osm`, `check_submap_spread`,
  `check_threads`, `check_limits`.

---

## Залежності й що можна паралелити

```
Ф0 ─┬─ Ф1 (read) ──────────┬─ Ф3 ── Ф4 ── Ф5 ⇦ ПЕРЕМИКАННЯ ─┬─ Ф6
    └─ Ф2 (параметризація) ─┘                                ├─ Ф7
                                                             └─ Ф8
```

Ф1 і Ф2 незалежні — можна робити поспіль без очікування. Усе після Ф5
паралелиться вільно.

## Ризики

| Ризик | Пом'якшення |
|:--|:--|
| Карта змінилась після 03.08 і знімок Ф1 не збігається з очікуваним | Ловиться **до** ручної роботи Ф3; `diff_route_revisions` покаже, що саме |
| Розкол прози (Ф3) виявиться дорожчим за оцінку | Партії по 5–8 із показом; можна зупинитись і перерозподілити |
| Гейт 2 не сходиться | Не перемикаємось. Старий репо робочий, розбираємось за позиціями |
| Треба змінити маршрут посеред міграції | Робимо **старим репо**, міграцію переганяємо на новий стан карти — гейт 2 від цього тільки чесніший |
| Воркер оновиться під час робіт | `server_tools_rev` у Ф1 і в `/trip`; перепідключення `/mcp` |

## Що потрібно від тебе

1. 👤 **Зараз:** «починай» — і я роблю Ф0 + Ф1 (обидві безпечні: перша нічого не
   переносить, друга нічого не пише).
2. 👤 **У Ф1:** звірити `map_id` травневої карти через `list_my_maps`.
3. 👤 **У Ф3:** приймати партії розколотих карток.
4. 👤 **У Ф5:** сказати, коли остання зміна пройшла старим репо — і подивитись на
   порожній план разом зі мною.
